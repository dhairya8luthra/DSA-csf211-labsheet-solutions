#include <stdio.h>

int main()
{
    // taking input
    int dim;
    scanf("%d", &dim);
    //
    int matrix[dim][dim];
    int visited[dim][dim];
    int number = 1;
    int spiral = 0;
    int i = 0;
    int j = 0;

    while (spiral != dim * dim)
    {
        if ()
    }
}